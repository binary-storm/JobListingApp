import 'package:flutter/material.dart';

class Job {
  String imageURL;
  String jobTitle;
  String companyName;
  int stepLeave;

  Job(this.imageURL, this.jobTitle, this.companyName, this.stepLeave);
}